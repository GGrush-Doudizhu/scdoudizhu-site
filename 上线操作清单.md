# 上线操作清单

## 我不能替你完成的事情

### 1. 使用你的 GitHub 密码

你已经在聊天里暴露过 GitHub 密码。为了账号安全，请立刻修改 GitHub 密码。

我不会使用、保存或尝试登录这个密码。GitHub 正常推送也不应该使用账号密码，而应使用：

- GitHub 网页登录
- GitHub Desktop
- SSH key
- Personal Access Token
- GitHub CLI 浏览器授权

### 2. 创建 GitHub 远程仓库

本机没有安装 GitHub CLI `gh`，而且我不能用你的密码登录 GitHub，所以新仓库需要你亲手在 GitHub 网页创建。

建议仓库名：

```text
scdoudizhu-site
```

仓库所有者：

```text
GGrush-Doudizhu
```

### 3. 配置 GitHub Pages 和域名

你需要在新仓库中亲手设置：

```text
Settings -> Pages -> Source: GitHub Actions
Settings -> Pages -> Custom domain: scdoudizhu.com
Enforce HTTPS
```

如果旧仓库仍绑定 `scdoudizhu.com`，需要先从旧仓库解除绑定。

### 4. 修改阿里云 DNS

根域名：

```text
@ A 185.199.108.153
@ A 185.199.109.153
@ A 185.199.110.153
@ A 185.199.111.153
```

www：

```text
www CNAME GGrush-Doudizhu.github.io
```

可选 IPv6：

```text
@ AAAA 2606:50c0:8000::153
@ AAAA 2606:50c0:8001::153
@ AAAA 2606:50c0:8002::153
@ AAAA 2606:50c0:8003::153
```

## 新仓库创建后推送本地项目

在新仓库创建完成后，在 PowerShell 中进入：

```powershell
cd F:\screp\scdoudizhu-site
```

如果你决定把这个目录作为新仓库根目录：

```powershell
git init
git add .
git commit -m "Initial second season website"
git branch -M main
git remote add origin https://github.com/GGrush-Doudizhu/scdoudizhu-site.git
git push -u origin main
```

如果 GitHub 要求登录，请使用浏览器授权、Personal Access Token 或 SSH key，不要使用旧密码。

## 每次更新比赛数据

把 `scdoudizhu_repparser.exe` 导出的 zip 放入：

```text
data-source/imports/season-2/YYYYMMDD/
```

然后双击：

```text
tools/一键更新比赛数据.bat
```

确认构建成功后提交并推送：

```powershell
git add .
git commit -m "Update match data"
git push
```

