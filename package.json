{
  "name": "scdoudizhu-dsl2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate:data": "node scripts/validate-public-data.mjs",
    "validate:sponsors": "node scripts/validate-sponsorship-ledger.mjs",
    "build:match-reports": "node scripts/build-match-reports.mjs",
    "check:match-reports": "node scripts/build-match-reports.mjs --check",
    "build:preview-standings": "node scripts/build-preview-standings.mjs",
    "check:preview-standings": "node scripts/build-preview-standings.mjs --check",
    "optimize:images": "node scripts/optimize-images.mjs",
    "test:e2e": "pnpm run build && playwright test",
    "serve:test": "node scripts/serve-dist.mjs 4321",
    "capture": "node scripts/capture-preview.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "pnpm run validate:data && pnpm run validate:sponsors && pnpm run check && pnpm run format:check && pnpm run build"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.4",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@playwright/test": "1.62.1",
    "prettier": "3.9.6",
    "prettier-plugin-astro": "0.14.1",
    "sharp": "0.35.3",
    "typescript": "5.9.3"
  }
}
