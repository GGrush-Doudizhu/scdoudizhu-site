# 第十比赛日第二次更新：直播解说特别加分及姓名归并

更新基线为 `3329710`。依据管理员再次更新的 `match-data/20260909/20260909.json` 备注，以及实际存在的 `match-data/same_name.csv` 执行；比赛日期仍为 2026-09-09。用户消息中的 `match-data/same/_name.csv` 不存在。

## 确认来源和处理

新备注明确规定 open 因当日直播有解说，决定当日总共加 20 分。此裁定更新了首次发布时按 15 分封顶的结果。备份原输入后，录入 `workPointOverrides` 中的 open、20 分及公开原因，保留用户原始备注、hostPoints 与全部对局字段。

特别核定表示当日工作积分的最终总额，替代常规封顶结果；不会把 20 分再加到原有工作分上。只适用于明确指定的日期和人员。共用赛报模板显示原因、核定总额，并在工作积分列标明“特别核定”。未配置例外的人员仍采用常规规则；嘴哥当晚房主 4 分加主播 10 分，共 14 分。

姓名映射仅增加 `shovgong → shougong`，没有删除或修改其他关系。9 月 9 日第一盘的 12 分及一场地主胜利归入 shougong。原始录像及 JSON 玩家 ID 保留，公开赛报统一显示 shougong；FFS-DBS 没有新增归并关系。

## 独立核算结果

全部 10 个比赛日的 JSON 结构和录像存在性检查通过。本次 5 份录像再次独立解析，人员、队伍、种族、时长和退出事件均与 JSON 一致。全季独立核算覆盖逐日对局分、工作分、累计总分、场次、胜负、角色数据、工作天数和胜率。

| 项目 | 更新前 | 更新后 |
| --- | --- | --- |
| open 当日工作积分 | 15 | 20 |
| FFS-Open-1 累计积分／排名 | 72／第 20 | 77／第 18 |
| shougong 累计积分／排名 | 86／第 17 | 98／第 16 |
| shougong 场次／胜／负 | 15／9／6 | 16／10／6 |
| 管理数据唯一身份 | 54 | 53 |

仍为 10 个比赛日、71 盘、568 参赛人次；当日 5 盘、14 名参赛者、16 行每日积分，地主赢 2 盘、农民赢 3 盘。此前 9 天公开赛报与管理数据中的比赛日记录均与本次备份完全一致。所有掉线结果保持一致，年轻本周韩服首次掉线记 0 分，不触发禁赛。

前五名及统计未变化：lansoov 408、GGrush 331、fly 250、do''do 237、IKILllIII 233。前 25 名成员保持不变，第 25 名仍为白胖 26 分。FFS-no.1 排名改为 17，怕瓦落地为 19，stefsunli 为 20；积分不变。

统计截至仍为 `2026-09-09T22:03:00+08:00`。对上次审计中的 open 15 分及 shovgong 独立身份结论，以本次管理员更新和复核为准。

## 验证和发布

- `check:match-reports` 通过，四份输出由正式生成器生成。
- `pnpm verify` 通过：9 项计分测试、公开数据、赞助流水、类型、格式和构建；Astro 0 错误、0 警告。新增说明字段最初触发类型检查错误，补充显式字段检查后整套验证通过。
- `pnpm test:e2e` 桌面与手机共 50 项全部通过，耗时 13.9 秒；新增特别加分和归并后的榜单断言，保留历史回归。
- `git diff --check` 通过。

本次独有备份、来源哈希、录像解析、独立计分脚本和线上验收脚本位于 `.audit-tmp/matchday-20260909-revision2/`。对应提交部署后核对正式网站；线上实际结果写入此目录的 `live-results.json`，部署状态写入 `deployment.json`。

原有 `docs/kk-replay-repair-20260907.md`、`scripts/fix_kk_replays.py`、`scripts/test_fix_kk_replays.py` 保持原状，不纳入本次提交。
