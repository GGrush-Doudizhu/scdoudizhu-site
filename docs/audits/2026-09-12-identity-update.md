# 2026-09-12 姓名归并更新

管理员更新了 `match-data/same_name.csv`，要求同步本地和线上。本次依据新映射重算全部 11 个比赛日、80 盘，未新增比赛或修改原始赛果 JSON。

## 来源与修正

- `江江达好` 归并为 `mascot520`。
- `逗地主 剑圣 激情对战 战到最后一兵` 归并为 `剑圣`。原始 JSON 在“剑圣”后有两个空格，管理员 CSV 为一个空格；备份原 CSV 后在同一行补充双空格别名，保留用户提供的单空格别名，使精确匹配能处理这两种形式。
- 其他映射保持不变，无别名冲突、同盘重复身份或禁赛期间出场。
- 更新前四份输出、原始 CSV、修改前后输入 SHA-256、独立核算和检查日志保存在 `.audit-tmp/identity-20260912-1205/`。

## 结果与影响

| 选手 | 归并前积分 | 归并后积分 | 总场数 | 胜负 | 排名与段位 |
| --- | --- | --- | --- | --- | --- |
| 剑圣 | 24 + 20 | 44 | 7 | 5 胜 2 负 | 第 24，黄金 |
| mascot520 | 12 + 24 | 36 | 6 | 4 胜 2 负 | 第 29，黄金 |

9 月 11 日的独立参赛身份由 23 减至 21，仍为 9 盘、地主胜 2 盘、农民胜 7 盘。每日积分表和逐盘姓名同步归并，工作人员、工作积分和 DR.Yang 首次掉线计分均保持不变。全赛季身份由 61 减至 59，参赛人次仍为 640。

前五名及分数不变：GGrush 416、lansoov 414、do''do 270、fly 250、IKILllIII 233。白银及以上公开榜由 40 条个人记录变为 41 条：斗 第 主、逗地主羞大圣、zhendeniu 同为 10 分，并列第 39，完整公开；青铜统一说明行另计。统计截至仍为 2026-09-11 22:15（UTC+8），不改成姓名映射更新时刻。

之前 10 个比赛日的公开赛报与更新前逐项一致。本次重算独立核对全赛季积分、胜负、角色场次、掉线事件、排名和段位，避免仅把已生成积分机械相加。此前 9 月 11 日审计中的未归并状态为当时结果，本次记录说明后续修正。

## 本地验证

- 11 份原始赛果 JSON 的 SHA-256 均与上次发布时一致。
- 全赛季独立核算、四份生成数据的 `check:match-reports` 检查通过。
- `pnpm run verify` 通过：9 项计分测试、公开数据与赞助数据验证、Astro 类型、格式检查和静态构建。
- `pnpm run test:e2e` 的 64 项桌面及手机测试通过，新增断言覆盖两人归并后的日积分、逐盘出场数、21 人赛报及第 39 名完整并列组。
- `git diff --check` 通过。原有录像修复文档和两份 Python 脚本保留原状，不纳入本次提交。
