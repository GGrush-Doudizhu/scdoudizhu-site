# DSL2 新站实施路线

## 阶段 0：隔离与考古

状态：已完成。

- 建立 `D:\scdoudizhu-dsl2` 独立 Git 工作区。
- 明确所有历史目录为只读来源。
- 理清旧仓库、个人站、当前线上原型和域名关系。
- 复制并校验 DSL2 方案与 DNS 基线。
- 形成产品需求和公开/私有数据边界。

完成证据见 Git 提交历史和 `docs/audits`。

## 阶段 1：赛事规则与内容冻结

状态：进行中。

- 主办方确认实际十周日期。
- 确认同分名次、黄金/白银边界和资格并列规则。
- 确认积分更新、错录、改判和补录流程。
- 确认 DSL2 正式地图版本、联系方式和品牌名称。
- 对暂未确定的季后赛细节建立带状态的配置，不阻塞公开站骨架开发。

完成标准：`docs/open-questions.md` 中“上线前必须确认”的问题全部有答案。

## 阶段 2：公开网站基础

状态：待开始。

- 从空目录初始化 Astro 和 TypeScript，不复制旧组件。
- 建立全局视觉系统、页面布局和响应式导航。
- 建立首页、积分榜、赛程、规则、季后赛、通知、奖励与赞助、地图和 404 页面。
- 建立明确的待公布、无积分和无通知状态。
- 加入 favicon、站点地图、robots、canonical 和分享元数据。

完成标准：所有页面可在无正式积分数据的情况下正确构建、阅读和导航。

## 阶段 3：私有计分工作区

状态：待开始。

- 创建 `D:\scdoudizhu-dsl2-operations` 独立 Git 仓库。
- 写入访问边界、数据保留、备份和忽略规则。
- 定义选手、别名、比赛日、对局、赛事工作、积分事件和修正模型。
- 实现规则版本化、确定性汇总和白名单公开导出。
- 使用合成测试数据覆盖地主/农民胜负、工作人员叠加、别名合并和修正场景。

完成标准：同一私有提交重复导出得到逐字节相同的公开结果，并能追溯每一分的来源。

## 阶段 4：公开数据接入与自动验证

状态：待开始。

- 确定公开 JSON 契约。
- 在网站构建前校验字段、积分、名次、段位、时间和禁用字段。
- 建立积分榜和首页前列选手的静态渲染。
- 扫描构建产物，防止原始回放、底层 ID 或私有备注泄露。
- 每次积分发布在私有仓库和公开仓库分别形成可追踪提交。

完成标准：错误或越界数据无法进入正式构建。

## 阶段 5：视觉、内容与质量验收

状态：待开始。

- 在不依赖未授权图片的情况下完成第一版星际赛事视觉语言。
- 校对赛制文案、金额、时间和术语。
- 完成桌面端、手机端、键盘、颜色对比和关闭 JavaScript 检查。
- 完成链接、HTML、性能、分享卡片和主要浏览器检查。
- 邀请主办方在预览地址逐页验收。

完成标准：产品需求中的第一版验收标准全部通过。

## 阶段 6：部署与域名切换

状态：待开始。

- 新建或确认目标 GitHub 仓库和公开权限。
- 配置 GitHub Actions 与 GitHub Pages 预览。
- 记录旧站最后线上版本、仓库和提交哈希。
- 在预览通过后设置新仓库自定义域名。
- 验证 HTTPS、根域名、`www` 跳转、404、缓存和分享信息。
- 保留可快速切回旧站的步骤，观察一段时间后再归档旧站。

完成标准：`scdoudizhu.com` 稳定服务新站，旧站仍可通过提交哈希恢复。

## 阶段 7：DSL1 历史档案

状态：后续项目，不阻塞 DSL2 上线。

- 确认 46 个比赛日各自的赛事归属。
- 以 `verified_data.csv` 为依据重建完整历史，而不是继续信任旧顶层汇总。
- 清理并记录重复、缺失和异常回放。
- 重新确认赞助者、头像和个人信息的公开授权。
- 只发布经过确认、标注统计范围且不会与 DSL2 积分混淆的历史内容。
